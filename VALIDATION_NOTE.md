# Validation note

The primary analysis mode and the nested-tuning mode were executed separately against the supplied `student-mat.csv` dataset. The generated summary tables, confusion matrices, original-study comparison, permutation-importance results, nested-CV summary and selected-hyperparameter records matched the precomputed files supplied in `results/`.

The dissertation document was rendered to PDF and visually inspected across all 30 pages after the final text and formatting revisions.
